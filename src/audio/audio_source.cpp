#include "pch.hpp"
#include "audio/audio_source.hpp"

Ref<AudioSource> AudioSource::Create(const std::filesystem::path &path)
{
    // do shit
}

Ref<AudioSource> AudioImporter::ImportAudioSource(const AssetHandle handle, const std::filesystem::path &path)
{
    // AudioSource::Create()
}